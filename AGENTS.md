# AGENTS.md

## Cursor Cloud specific instructions

This repository (`GestaltAgenticSoftwareOperator`) is currently empty — it contains only a `README.md` placeholder. There are no application services, dependencies, build steps, lint checks, or tests to run.

### Current state

- **No source code or application logic** exists yet.
- **No dependency files** (`package.json`, `requirements.txt`, `go.mod`, etc.).
- **No Docker / docker-compose configuration**.
- **No CI/CD pipelines**.
- **No devcontainer configuration**.

### When code is added

Once application code is introduced, this section should be updated with:

- How to install dependencies (update script).
- How to run lint, tests, and the dev server.
- Any non-obvious startup caveats or environment requirements.
